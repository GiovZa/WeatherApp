<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Theme.MyFirstApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand colors -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand colors -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Background color attribute -->
        <item name="backgroundColor">@color/white</item>
    </style>

    <!-- Light Mode Theme -->
    <style name="AppTheme.Light" parent="Theme.MyFirstApp">
        <item name="backgroundColor">@color/white</item>
        <item name="android:windowBackground">@color/white</item>
    </style>

    <!-- Dark Mode Theme -->
    <style name="AppTheme.Dark" parent="Theme.MyFirstApp">
        <item name="backgroundColor">@color/black</item>
        <item name="android:windowBackground">@color/black</item>
    </style>

    <!-- Red Theme -->
    <style name="AppTheme.Red" parent="Theme.MyFirstApp">
        <item name="backgroundColor">#FF0000</item>
        <item name="android:windowBackground">#FF0000</item>
    </style>

    <!-- Blue Theme -->
    <style name="AppTheme.Blue" parent="Theme.MyFirstApp">
        <item name="backgroundColor">#0000FF</item>
        <item name="android:windowBackground">#0000FF</item>
    </style>

</resources>